Homepage testimonial + hero-badge photos.

Drop the three friends' photos in THIS folder with these exact names:

  alex.jpg      (Alex's photo — the night / rooftop shot)
  marcus.jpg    (Marcus T.'s photo — the on-stage shot)
  ryan.jpg      (Ryan's photo — the home-studio / cap shot)

- Any of .jpg / .jpeg / .png works, but keep the name (e.g. alex.jpg). If you use a
  different extension, update the paths in app/page.tsx (the `photo:` fields and the
  hero badge).
- Roughly square images look best (they're shown as circles). ~400x400px is plenty.
- Until the files exist, the site shows tasteful initial circles (A / M / R) instead
  of broken images — so nothing looks broken.

Only use photos of people who actually gave the feedback (or gave permission). Pairing
stock photos of strangers with named reviews is what gets flagged as fake endorsements.
