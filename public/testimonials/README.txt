Homepage photos — drop the image files in THIS folder with these exact names.

FEEDBACK CARDS ("Players on Tonefex") — the 3 individual photos:
  alex.jpg      (the home-studio / brown-hoodie shot)
  marcus.jpg    (the on-stage, blue-light shot)
  ryan.jpg      (the night rooftop / in-ear-monitors shot)

HERO TRUST BADGE (top of the homepage) — the 3 badge photos:
  badge-1.jpg
  badge-2.jpg
  badge-3.jpg
  (If you have them as one combined/collage image, crop it into 3 separate square
   files first — the badge shows 3 small round faces.)

Notes:
- .jpg / .jpeg / .png all work, but keep the name (e.g. alex.jpg). If you use a
  different extension, update the paths in app/page.tsx (the testimonials `photo:`
  fields and the badgeAvatars array).
- Roughly square images look best (shown as circles). ~400x400px is plenty.
- Until a file exists, that slot shows a tasteful gradient circle instead of a broken
  image — so nothing ever looks broken.

Only use photos of people who actually gave the feedback (or gave permission). Pairing
stock photos of strangers with named reviews is what gets flagged as fake endorsements.
